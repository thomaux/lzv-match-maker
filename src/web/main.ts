import { App } from './app/App';
import Vue from 'vue';

// tslint:disable-next-line: no-unused-expression
new Vue({
    el: '#app',
    template: '<app></app>',
    components: {
        App
    }
});
